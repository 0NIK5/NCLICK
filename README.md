# NCLICK
NOIZE COUNTER (EXPANDER)
NCLICK: Счетчик Эспандера с Анализатором Звука
NCLICK — это веб-приложение, которое использует микрофон вашего устройства и технологию Web Audio API для точного подсчета повторений при работе с ручным эспандером (кистевым тренажером).

Главная особенность NCLICK — настраиваемый полосно-пропускающий фильтр (Band-Pass Filter), который позволяет изолировать звук "щелчка" эспандера от посторонних шумов (голоса, музыки, гула компьютера).

✨ Возможности
Точный подсчет: Регистрирует только резкие звуковые события (клики) после настройки чувствительности.

Спектральный анализ: Визуальный график спектра звука в реальном времени, помогающий определить частоту щелчка (в Гц).

Band-Pass фильтр (Low-Cut / High-Cut): Два ползунка для создания узкого "окна" частот, что устраняет ложные срабатывания от низкочастотного и высокочастотного шума.

Настраиваемая цель: Возможность установить целевое количество повторений и получить звуковое оповещение по достижении.

Кроссплатформенность: Работает прямо в браузере (Chrome, Firefox, Edge) без установки.

🚀 Как запустить
Сохраните файл: Скопируйте весь код, включая HTML, CSS и JavaScript, и сохраните его в файл с именем, например, nclick.html.

Откройте в браузере: Откройте файл nclick.html в современном браузере (Google Chrome предпочтителен).

Разрешите доступ: Нажмите кнопку "▶ Включить микрофон" и разрешите браузеру использовать микрофон.

🛠️ Руководство по настройке
Для достижения максимальной точности необходимо выполнить 4 шага настройки.

Шаг 1: Анализ частоты щелчка
Перед настройкой фильтров определите, на какой частоте "щелкает" ваш эспандер.

Установите НИЖНЮЮ ГРАНИЦУ (Low-Cut) на минимальное значение (500 Hz).

Установите ВЕРХНЮЮ ГРАНИЦУ (High-Cut) на максимальное значение (20000 Hz).

Сделайте несколько щелчков эспандером и обратите внимание на цифру "Пиковая частота (Max Hz)". Обычно она находится в диапазоне 2000 Hz — 5000 Hz.

Шаг 2: Настройка НИЖНЕЙ ГРАНИЦЫ (Low-Cut Filter)
Этот фильтр отсекает низкие частоты (гул компьютера, шум улицы, басы).

Ползунок 1. НИЖНЯЯ ГРАНИЦА (High-Pass): Установите его чуть ниже вашей пиковой частоты (например, если пик 2500 Hz, установите 2000 Hz).

Цель: Игнорировать весь низкочастотный фоновый шум.

Шаг 3: Настройка ВЕРХНЕЙ ГРАНИЦЫ (High-Cut Filter)
Этот фильтр отсекает высокие частоты, которые могут быть вызваны, например, звоном ключей или ультразвуком.

Ползунок 2. ВЕРХНЯЯ ГРАНИЦА (Low-Pass): Установите его чуть выше вашей пиковой частоты (например, если пик 2500 Hz, установите 3500 Hz).

Цель: Создать узкое "окно" Band-Pass, сфокусированное только на звуке эспандера.

Шаг 4: Настройка Порога Громкости
После фильтрации осталось только то, что нужно. Теперь настройте чувствительность.

Ползунок 3. Порог громкости (Чувствительность): Начните с низкого значения и постепенно увеличивайте, пока счетчик не перестанет срабатывать от тихого щелчка, но стабильно срабатывал от вашего нормального сжатия.

Наблюдайте за синей полоской: она должна перепрыгивать красную линию только при щелчке.

⚙️ Технические детали (Web Audio API)
Приложение построено на нативной технологии браузера и использует три основных узла для точной обработки сигнала:

HighpassFilter (BiquadFilterNode): Отсекает частоты ниже установленной "Нижней границы".

LowpassFilter (BiquadFilterNode): Отсекает частоты выше установленной "Верхней границы".

AnalyserNode: Получает уже отфильтрованный звуковой сигнал, вычисляет его общую громкость (averageVol) для счетчика и строит спектральный график для визуализации.

Цепочка обработки: Микрофон → Low-Cut Filter → High-Cut Filter → Analyser → Логика подсчета.
